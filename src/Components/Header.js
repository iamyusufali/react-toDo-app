import React from 'react';
import { TitleWrapper, Title } from '../StyledComponents/StyledTitle';

const Header = () => {
  return (
   <TitleWrapper>
     <Title>ToDo App</Title>
   </TitleWrapper>
  )
}

export default Header;